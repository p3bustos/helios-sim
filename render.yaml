services:
  - type: web
    name: helios-sim-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    envVars:
      - key: WEATHER_API_KEY
        sync: false
      - key: PORT
        value: 8080